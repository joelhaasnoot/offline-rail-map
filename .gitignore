# Gradle / Android
.gradle/
build/
local.properties
*.iml
.idea/
captures/
.kotlin/

# Release signing secrets and packaged releases (see scripts/package-android-release.sh
# and scripts/package-ios-release.sh)
keystore.properties
*.jks
*.keystore
android/dist/
signing.properties
ios/Config/Signing.local.xcconfig
ios/dist/
*.p8

# Xcode / Swift Package Manager
xcuserdata/
DerivedData/
.build/
.swiftpm/

# Pipeline working data (large downloads, databases, generated tiles)
pipeline/work/
pipeline/out/

# Generated app assets (style, legend, sprites, fonts) are committed so a clone builds;
# regenerate them with pipeline/prepare_style.py after upstream style changes.

# Python
__pycache__/

# macOS
.DS_Store
